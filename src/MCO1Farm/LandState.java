package MCO1Farm;

/**
 * Enum class to store all possible states
 * that TextLand can be in.
 */

enum LandState {
    BLOCKED,
    UNPLOWED,
    PLOWED,
    PLANTED,
    WITHERED,
    HARVESTABLE;
}