package MyFarm.land;

/**
 * Enum class to store all possible states
 * that TextLand can be in.
 */

public enum LandState {
    BLOCKED,
    UNPLOWED,
    PLOWED,
    PLANTED,
    WITHERED,
    HARVESTABLE;
}